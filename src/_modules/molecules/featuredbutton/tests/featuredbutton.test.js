'use strict';

var Featuredbutton = require('../featuredbutton');

describe('Featuredbutton View', function() {

  beforeEach(function() {
    this.featuredbutton = new Featuredbutton();
  });

  it('Should run a few assertions', function() {
    expect(this.featuredbutton);
  });

});
