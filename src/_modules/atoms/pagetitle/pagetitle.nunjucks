{% macro content(spec) %}

{% set spec = spec or {} %}
{% set _class = spec.class or 'page-title green-underline' %}
{% set _text = spec.text or 'Page Title' %}

<h1 class="{{_class}}">{{_text}}</h1>

{% endmacro %}
