'use strict';

var Featuredbuttons = require('../featuredbuttons');

describe('Featuredbuttons View', function() {

  beforeEach(function() {
    this.featuredbuttons = new Featuredbuttons();
  });

  it('Should run a few assertions', function() {
    expect(this.featuredbuttons);
  });

});
