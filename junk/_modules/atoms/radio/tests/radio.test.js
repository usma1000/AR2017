'use strict';

var Radio = require('../radio');

describe('Radio View', function() {

  beforeEach(function() {
    this.radio = new Radio();
  });

  it('Should run a few assertions', function() {
    expect(this.radio);
  });

});
